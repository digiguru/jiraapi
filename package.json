{
  "name": "jiraapi",
  "version": "0.1.0",
  "private": true,
  "description": "Small compatibility proxy over Jira search queries, deployable to Vercel.",
  "type": "module",
  "dependencies": {
    "express": "^5.2.1",
    "jira-client": "^8.2.2"
  },
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "lint": "eslint .",
    "build": "node scripts/build-check.mjs",
    "test:ci": "node --test",
    "test": "node --test"
  },
  "devDependencies": {
    "eslint": "^10.8.0"
  }
}
